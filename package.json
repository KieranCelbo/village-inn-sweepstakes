{
  "name": "village-inn-sweepstakes-proxy",
  "version": "1.0.0",
  "description": "Proxy server for Village Inn Sweepstakes \u2014 Betfair odds + Racing API",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "start:local": "node -r dotenv/config proxy.js",
    "dev": "node -r dotenv/config proxy.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "firebase-admin": "^12.0.0"
  }
}