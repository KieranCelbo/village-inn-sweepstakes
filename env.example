# Copy this file to .env and fill in your credentials
# Run locally with: node -r dotenv/config proxy.js

# The Racing API (free tier)
RACING_API_USER=your-racing-api-username
RACING_API_PASS=your-racing-api-password

# Betfair Exchange (free delayed app key)
BETFAIR_USER=your@betfair-email.com
BETFAIR_PASS=your-betfair-password
BETFAIR_APP_KEY=your-1.0-delay-app-key

# Firebase Admin (for auto-refresh of odds + results)
# Option A (local): place serviceAccountKey.json in this folder
# Option B (Railway): paste the entire JSON content of serviceAccountKey.json here
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"cheltenham-sweepstakes",...}
